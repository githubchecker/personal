---
trigger: on_demand
description: "Shared module — [Feature]_spec.md output template for DISCOVERY Phase 6. Load when workflow_01_discovery_ref.md says to."
---

# Output Format: `[Feature]_spec.md`

> **HEADER MANDATE:** Fill every field. VALIDATOR auto-detects mode (PORTING/VERSIONED/Q&A+CODEBASE/SPEC-ONLY) from the `Source:` field — a blank or vague Source: forces an ambiguity prompt before analysis starts. Fill every bracketed field.

```markdown
# [Feature Name] — Specification
**Version:** [n] | **Status:** SPEC_FINAL | **Gate:** GO
**Gate condition for implementation:** [e.g., "after Phase 0 prerequisites complete" or "all CRITICAL CFs resolved" or "N/A"]
**Generated by:** DISCOVERY V3.1 | [date]
**Source:** [one of the four values below — pick the one that matches:]
  Q&A-ONLY                              ← spec built entirely from conversation, no external document
  Q&A+CODEBASE:[comma-separated project names]  ← conversation + codebase context (no doc)
  DOCUMENT:[filename + version/date]    ← ported from an existing document (sets Porting Mode: ACTIVE)
  VERSIONED:[previous spec filename]    ← this spec extends a previous spec version
**Codebase:** [PRESENT — projects read: X, Y, Z  |  ABSENT — no codebase available]
**Porting Mode:** [ACTIVE — artefacts reproduced verbatim  |  NOT ACTIVE]
**Sessions:** [n] — [brief description, e.g. "Session 1: initial Q&A | Session 2: added auth flow"]
**Cross-checked:** [yes — model used / SKIPPED — risk acknowledged]
**Summary:** REQ=[n] BUG=[n] (existing=[n] new-design=[n]) CF=[n] (CRITICAL=[n]) OQ=[n] DEC=[n]

## 1. Business Context & Architecture

### 1.1 Business Context
[WHY — domain rationale, business motivation. 2–5 sentences. No code.]

### 1.2 Architecture & Data Flow Map
- Modules involved: [List class/module names]
- Data Flow: [Source] -> [Process] -> [Sink]
- Key Patterns: [e.g., Event-Driven, Singleton, Repository]

## 2. Capability Matrix & Requirements

### Capability Matrix
| REQ-ID | Requirement | Status | Evidence |
|--------|-------------|--------|----------|
| REQ-001 | [what, not how] | EXISTS / PARTIAL / MISSING | [file:fn or "new build"] |

> **EXISTS rows (PORTING RULE):** append `Context: [key class/method/flow]` to the Evidence cell — e.g. `Context: RedisService.RegisterDeviceLoginAsync`. Mandatory.

### Bug & Technical Debt Register
| BUG-ID | Source | File : Location | Severity | Description | Blocks REQ-ID | Fix Phase |
|--------|--------|----------------|----------|-------------|---------------|-----------|
| BUG-001 | EXISTING | `Service.cs` : `Method()` | 🔴/🟡/🟢 | [what is wrong] | REQ-XXX | P0 |
| BUG-002 | NEW_FILE_DESIGN | `NewClass.cs` : constructor | 🟡 | [design bug in proposed code] | REQ-XXX | P[n] |

> Source values: `EXISTING` = current codebase bug. `NEW_FILE_DESIGN` = bug in a proposed (not-yet-existing) file's design. `Blocks REQ-ID` = what ships broken if unfixed. `Fix Phase` = PLANNER uses this for task ordering.

### Recommended Refactoring (Non-Blocking)
| Refactor-ID | Location | Description | Benefit | Complexity |
|-------------|----------|-------------|---------|------------|
| REF-001 | `Service.cs` | Rename `DoIt()` to `ExecuteTrade()` | Readability | SMALL |

## 3. Architecture Decisions (Pre-Agreed — PLANNER does not re-decide these)
> Every MISSING REQ-ID must have a DEC-ID here.
> PORTING MODE: reproduce all code blocks, field lists, constants verbatim — do NOT summarise.

### DEC-001: [Decision Name]
**Decision:** [exact technical choice — specific enough that PLANNER writes code directly from this]
**Why:** [rationale]
**Alternative rejected:** [what was considered and exactly why it was rejected]
**Codebase evidence:** [file:function or "new build — no existing pattern"]
**Risk:** LOW / MED / HIGH | **Complexity:** S / M / L / XL

> **CONTEXT-ONLY DEC-IDs (PORTING MODE):** For EXISTS capabilities with a journey or decision table in the source, add:
> `### DEC-XXX: [Capability name] [CONTEXT-ONLY — no implementation work implied]`
> Preserves design history for PLANNER's reference. **PLANNER MUST NOT generate tasks from CONTEXT-ONLY DEC-IDs.**

## 4. Technical Constraints & Frozen Values
[Constants, thresholds, invariants — exact values from codebase or explicitly stated as new.]
[Include complete class/file bodies for complex constant sets. Do NOT paraphrase.]

## 5. QA & Verification Strategy
- Test Harness: [e.g., "Use existing ReplayEngine" or "Build new MockConnector"]
- Data Requirements: [e.g., "Requires 1 week of Tick Data in /data/raw"]
- Manual Verification: [What must be checked by human eye?]

## 6. Implementation Phase Plan
| Phase | Gate | Scope |
|-------|------|-------|
| Phase 0 | None — start immediately | Bug fixes |
| Phase 1 | Phase 0 done | [what] |

### Phase Dependencies
[What must be done before what — with specific REQ-ID and BUG-ID references]

### Critical Findings Bound to Phases
| CF-ID | Severity | Binds to Phase / Task | Type |
|-------|----------|-----------------------|------|
| CF-001 | 🔴 CRITICAL | Phase 1 — rolling queues task | GATE — task cannot complete without CF fix verified |
| CF-002 | 🟡 HIGH | Phase 3 — exporter task | ACCOMPANIES — CF fix in initial implementation |

## 7. Critical Findings Register (CF)
[Implementation traps in proposed designs — NOT bugs in existing code, NOT feature gaps.]
| CF-ID | Severity | Phase | Finding | Fix |
|-------|----------|-------|---------|-----|
| CF-001 | 🔴 CRITICAL | P[n] | [what will fail and why] | [exact fix] |

[If zero:] **CF REGISTER: CLEAR — no implementation traps found.**

## 8. Feature Index (if applicable)
[Clean sequential integer IDs confirmed by user. No sub-indices. No gaps.]

## 9. Open Questions
### OQ-001: [Question]
**Blocks:** [REQ-XXX / CF-XXX / Phase N]
**Options:** A: [desc + tradeoff] / B: [desc + tradeoff]
**Recommended:** [A/B/None — one-sentence reasoning]
**Status:** OPEN / ANSWERED: [answer]

## 10. Risk Register
| RISK-ID | Probability | Impact | Description | Mitigation |
|---------|-------------|--------|-------------|------------|

## 11. Hallucination Guard — Verified Claims
| Claim | Verified in | Confidence |
|-------|------------|------------|
| [claim about existing code] | [file:fn] | HIGH |
| [claim not yet verified] | UNVERIFIED — check at P[n] | LOW |

## 12. Project Decision Dashboard
[Copied verbatim from Phase 4C output]

## 13. Implementation Reference (PORTING MODE only)
[One §13.N subsection per topic. Full scope is defined in `workflow_01_discovery.md` Phase 3 — the five PORTING RULES. Mandatory content: code artefacts, user journeys (ALL features), "What Currently Exists" sections, env var tables, decision option tables WITH rationale sentences (WHY rejected — not just chosen option), explicitly named gaps, "What Is Broken or Missing" tables.]
```
